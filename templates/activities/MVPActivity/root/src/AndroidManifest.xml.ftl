<manifest xmlns:android="http://schemas.android.com/apk/res/android" >

    <application>
        <activity android:name="${featureOut}.${capFeatureName}Activity"
			android:screenOrientation="portrait"
            android:theme="@style/${appTheme}" />
    </application>
</manifest>
