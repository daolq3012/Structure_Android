package com.ccc.nameapp.utils

object Constant {
    object Keyboard {
        const val MIN_HEIGHT_VISIBLE = 100
    }

    object Environment {
        const val DEV_PACKAGE = "dev"
        const val STG_PACKAGE = "stg"
    }
}
